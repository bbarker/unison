# Keyword Prefix Identifier Parse Success Fixture

``` ucm :hide
> builtins.mergeio lib.builtins
```

``` unison
use lib.builtins

ifx = 42

type then0 a = Then0 a
```

``` ucm
> add
> view ifx
> view then0
```
